config.json
credentials.json
tracker.db

# The data plane. Git is not a sync or backup layer: the server serves these
# over the API and restic backs them up encrypted+offsite. logs/ held personal
# writing and backups/ held full SQL dumps of the database — both were pushed
# to GitHub on every edit.
logs/
backups/
daybook/
__pycache__/
*.pyc

# PyInstaller build artifacts (run build_exe.bat to regenerate the .exe)
build/
dist/
*.spec
!productivity-tracker.spec

# Cloudflare Worker
QR-accountability/.env
QR-accountability/.wrangler/
.wrangler/
node_modules/

# Reference material and scratch that should never be in the repo
*.pdf
Getting Things Done — condensed.md
career-discovery/
ea-socials/
*.m4a
*.lnk
QR-accountability/temp-verify*/
.oci/
*.pem
run-client.command

# Personal & medical records — never commit. Consolidated out of the repo root.
personal/

# macOS
.DS_Store

# Needs `gh auth refresh -s workflow` before it can be pushed (Worker CI).
.github/
